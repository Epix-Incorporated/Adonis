name: Greetings

on: [pull_request_target, issues]

jobs:
  greeting:
    runs-on: ubuntu-latest
    permissions:
      issues: write
      pull-requests: write
    steps:
    - uses: actions/first-interaction@v1
      with:
        repo-token: ${{ secrets.GITHUB_TOKEN }}
        issue-message: "👋 Welcome! It looks like this is your first time creating an issue here. Do expect a swift response from a maintainer. Make sure you've included all necessary details in your post!"
        pr-message: "👋 Hello there! It looks like this is your first time contributing to the Adonis project. Remember to have read the Contribution Guidelines! They're super-important. Good luck!"
